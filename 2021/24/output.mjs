// @flow
export function program(input: number[]): {| w:number,x:number,y:number,z:number|} {

    let w = 0;
    let x = 0;
    let y = 0;
    let z = 0;
  
/* inp w */

          {
            const temp = input.shift();
            if (temp === undefined) {
              throw new Error('Not enough input!');
            }
            w = temp;
          }
        
/* mul x 0 */
x *= 0;
/* add x z */
x += z;
/* mod x 26 */
x %= 26;
/* div z 1 */

          {
            const temp = z / 1;
            z = temp >= 0 ? Math.floor(temp) : Math.ceil(temp);
          }
        
/* add x 11 */
x += 11;
/* eql x w */
x = x === w ? 1 : 0;
/* eql x 0 */
x = x === 0 ? 1 : 0;
/* mul y 0 */
y *= 0;
/* add y 25 */
y += 25;
/* mul y x */
y *= x;
/* add y 1 */
y += 1;
/* mul z y */
z *= y;
/* mul y 0 */
y *= 0;
/* add y w */
y += w;
/* add y 3 */
y += 3;
/* mul y x */
y *= x;
/* add z y */
z += y;
/* inp w */

          {
            const temp = input.shift();
            if (temp === undefined) {
              throw new Error('Not enough input!');
            }
            w = temp;
          }
        
/* mul x 0 */
x *= 0;
/* add x z */
x += z;
/* mod x 26 */
x %= 26;
/* div z 1 */

          {
            const temp = z / 1;
            z = temp >= 0 ? Math.floor(temp) : Math.ceil(temp);
          }
        
/* add x 14 */
x += 14;
/* eql x w */
x = x === w ? 1 : 0;
/* eql x 0 */
x = x === 0 ? 1 : 0;
/* mul y 0 */
y *= 0;
/* add y 25 */
y += 25;
/* mul y x */
y *= x;
/* add y 1 */
y += 1;
/* mul z y */
z *= y;
/* mul y 0 */
y *= 0;
/* add y w */
y += w;
/* add y 7 */
y += 7;
/* mul y x */
y *= x;
/* add z y */
z += y;
/* inp w */

          {
            const temp = input.shift();
            if (temp === undefined) {
              throw new Error('Not enough input!');
            }
            w = temp;
          }
        
/* mul x 0 */
x *= 0;
/* add x z */
x += z;
/* mod x 26 */
x %= 26;
/* div z 1 */

          {
            const temp = z / 1;
            z = temp >= 0 ? Math.floor(temp) : Math.ceil(temp);
          }
        
/* add x 13 */
x += 13;
/* eql x w */
x = x === w ? 1 : 0;
/* eql x 0 */
x = x === 0 ? 1 : 0;
/* mul y 0 */
y *= 0;
/* add y 25 */
y += 25;
/* mul y x */
y *= x;
/* add y 1 */
y += 1;
/* mul z y */
z *= y;
/* mul y 0 */
y *= 0;
/* add y w */
y += w;
/* add y 1 */
y += 1;
/* mul y x */
y *= x;
/* add z y */
z += y;
/* inp w */

          {
            const temp = input.shift();
            if (temp === undefined) {
              throw new Error('Not enough input!');
            }
            w = temp;
          }
        
/* mul x 0 */
x *= 0;
/* add x z */
x += z;
/* mod x 26 */
x %= 26;
/* div z 26 */

          {
            const temp = z / 26;
            z = temp >= 0 ? Math.floor(temp) : Math.ceil(temp);
          }
        
/* add x -4 */
x += -4;
/* eql x w */
x = x === w ? 1 : 0;
/* eql x 0 */
x = x === 0 ? 1 : 0;
/* mul y 0 */
y *= 0;
/* add y 25 */
y += 25;
/* mul y x */
y *= x;
/* add y 1 */
y += 1;
/* mul z y */
z *= y;
/* mul y 0 */
y *= 0;
/* add y w */
y += w;
/* add y 6 */
y += 6;
/* mul y x */
y *= x;
/* add z y */
z += y;
/* inp w */

          {
            const temp = input.shift();
            if (temp === undefined) {
              throw new Error('Not enough input!');
            }
            w = temp;
          }
        
/* mul x 0 */
x *= 0;
/* add x z */
x += z;
/* mod x 26 */
x %= 26;
/* div z 1 */

          {
            const temp = z / 1;
            z = temp >= 0 ? Math.floor(temp) : Math.ceil(temp);
          }
        
/* add x 11 */
x += 11;
/* eql x w */
x = x === w ? 1 : 0;
/* eql x 0 */
x = x === 0 ? 1 : 0;
/* mul y 0 */
y *= 0;
/* add y 25 */
y += 25;
/* mul y x */
y *= x;
/* add y 1 */
y += 1;
/* mul z y */
z *= y;
/* mul y 0 */
y *= 0;
/* add y w */
y += w;
/* add y 14 */
y += 14;
/* mul y x */
y *= x;
/* add z y */
z += y;
/* inp w */

          {
            const temp = input.shift();
            if (temp === undefined) {
              throw new Error('Not enough input!');
            }
            w = temp;
          }
        
/* mul x 0 */
x *= 0;
/* add x z */
x += z;
/* mod x 26 */
x %= 26;
/* div z 1 */

          {
            const temp = z / 1;
            z = temp >= 0 ? Math.floor(temp) : Math.ceil(temp);
          }
        
/* add x 10 */
x += 10;
/* eql x w */
x = x === w ? 1 : 0;
/* eql x 0 */
x = x === 0 ? 1 : 0;
/* mul y 0 */
y *= 0;
/* add y 25 */
y += 25;
/* mul y x */
y *= x;
/* add y 1 */
y += 1;
/* mul z y */
z *= y;
/* mul y 0 */
y *= 0;
/* add y w */
y += w;
/* add y 7 */
y += 7;
/* mul y x */
y *= x;
/* add z y */
z += y;
/* inp w */

          {
            const temp = input.shift();
            if (temp === undefined) {
              throw new Error('Not enough input!');
            }
            w = temp;
          }
        
/* mul x 0 */
x *= 0;
/* add x z */
x += z;
/* mod x 26 */
x %= 26;
/* div z 26 */

          {
            const temp = z / 26;
            z = temp >= 0 ? Math.floor(temp) : Math.ceil(temp);
          }
        
/* add x -4 */
x += -4;
/* eql x w */
x = x === w ? 1 : 0;
/* eql x 0 */
x = x === 0 ? 1 : 0;
/* mul y 0 */
y *= 0;
/* add y 25 */
y += 25;
/* mul y x */
y *= x;
/* add y 1 */
y += 1;
/* mul z y */
z *= y;
/* mul y 0 */
y *= 0;
/* add y w */
y += w;
/* add y 9 */
y += 9;
/* mul y x */
y *= x;
/* add z y */
z += y;
/* inp w */

          {
            const temp = input.shift();
            if (temp === undefined) {
              throw new Error('Not enough input!');
            }
            w = temp;
          }
        
/* mul x 0 */
x *= 0;
/* add x z */
x += z;
/* mod x 26 */
x %= 26;
/* div z 26 */

          {
            const temp = z / 26;
            z = temp >= 0 ? Math.floor(temp) : Math.ceil(temp);
          }
        
/* add x -12 */
x += -12;
/* eql x w */
x = x === w ? 1 : 0;
/* eql x 0 */
x = x === 0 ? 1 : 0;
/* mul y 0 */
y *= 0;
/* add y 25 */
y += 25;
/* mul y x */
y *= x;
/* add y 1 */
y += 1;
/* mul z y */
z *= y;
/* mul y 0 */
y *= 0;
/* add y w */
y += w;
/* add y 9 */
y += 9;
/* mul y x */
y *= x;
/* add z y */
z += y;
/* inp w */

          {
            const temp = input.shift();
            if (temp === undefined) {
              throw new Error('Not enough input!');
            }
            w = temp;
          }
        
/* mul x 0 */
x *= 0;
/* add x z */
x += z;
/* mod x 26 */
x %= 26;
/* div z 1 */

          {
            const temp = z / 1;
            z = temp >= 0 ? Math.floor(temp) : Math.ceil(temp);
          }
        
/* add x 10 */
x += 10;
/* eql x w */
x = x === w ? 1 : 0;
/* eql x 0 */
x = x === 0 ? 1 : 0;
/* mul y 0 */
y *= 0;
/* add y 25 */
y += 25;
/* mul y x */
y *= x;
/* add y 1 */
y += 1;
/* mul z y */
z *= y;
/* mul y 0 */
y *= 0;
/* add y w */
y += w;
/* add y 6 */
y += 6;
/* mul y x */
y *= x;
/* add z y */
z += y;
/* inp w */

          {
            const temp = input.shift();
            if (temp === undefined) {
              throw new Error('Not enough input!');
            }
            w = temp;
          }
        
/* mul x 0 */
x *= 0;
/* add x z */
x += z;
/* mod x 26 */
x %= 26;
/* div z 26 */

          {
            const temp = z / 26;
            z = temp >= 0 ? Math.floor(temp) : Math.ceil(temp);
          }
        
/* add x -11 */
x += -11;
/* eql x w */
x = x === w ? 1 : 0;
/* eql x 0 */
x = x === 0 ? 1 : 0;
/* mul y 0 */
y *= 0;
/* add y 25 */
y += 25;
/* mul y x */
y *= x;
/* add y 1 */
y += 1;
/* mul z y */
z *= y;
/* mul y 0 */
y *= 0;
/* add y w */
y += w;
/* add y 4 */
y += 4;
/* mul y x */
y *= x;
/* add z y */
z += y;
/* inp w */

          {
            const temp = input.shift();
            if (temp === undefined) {
              throw new Error('Not enough input!');
            }
            w = temp;
          }
        
/* mul x 0 */
x *= 0;
/* add x z */
x += z;
/* mod x 26 */
x %= 26;
/* div z 1 */

          {
            const temp = z / 1;
            z = temp >= 0 ? Math.floor(temp) : Math.ceil(temp);
          }
        
/* add x 12 */
x += 12;
/* eql x w */
x = x === w ? 1 : 0;
/* eql x 0 */
x = x === 0 ? 1 : 0;
/* mul y 0 */
y *= 0;
/* add y 25 */
y += 25;
/* mul y x */
y *= x;
/* add y 1 */
y += 1;
/* mul z y */
z *= y;
/* mul y 0 */
y *= 0;
/* add y w */
y += w;
/* add y 0 */
y += 0;
/* mul y x */
y *= x;
/* add z y */
z += y;
/* inp w */

          {
            const temp = input.shift();
            if (temp === undefined) {
              throw new Error('Not enough input!');
            }
            w = temp;
          }
        
/* mul x 0 */
x *= 0;
/* add x z */
x += z;
/* mod x 26 */
x %= 26;
/* div z 26 */

          {
            const temp = z / 26;
            z = temp >= 0 ? Math.floor(temp) : Math.ceil(temp);
          }
        
/* add x -1 */
x += -1;
/* eql x w */
x = x === w ? 1 : 0;
/* eql x 0 */
x = x === 0 ? 1 : 0;
/* mul y 0 */
y *= 0;
/* add y 25 */
y += 25;
/* mul y x */
y *= x;
/* add y 1 */
y += 1;
/* mul z y */
z *= y;
/* mul y 0 */
y *= 0;
/* add y w */
y += w;
/* add y 7 */
y += 7;
/* mul y x */
y *= x;
/* add z y */
z += y;
/* inp w */

          {
            const temp = input.shift();
            if (temp === undefined) {
              throw new Error('Not enough input!');
            }
            w = temp;
          }
        
/* mul x 0 */
x *= 0;
/* add x z */
x += z;
/* mod x 26 */
x %= 26;
/* div z 26 */

          {
            const temp = z / 26;
            z = temp >= 0 ? Math.floor(temp) : Math.ceil(temp);
          }
        
/* add x 0 */
x += 0;
/* eql x w */
x = x === w ? 1 : 0;
/* eql x 0 */
x = x === 0 ? 1 : 0;
/* mul y 0 */
y *= 0;
/* add y 25 */
y += 25;
/* mul y x */
y *= x;
/* add y 1 */
y += 1;
/* mul z y */
z *= y;
/* mul y 0 */
y *= 0;
/* add y w */
y += w;
/* add y 12 */
y += 12;
/* mul y x */
y *= x;
/* add z y */
z += y;
/* inp w */

          {
            const temp = input.shift();
            if (temp === undefined) {
              throw new Error('Not enough input!');
            }
            w = temp;
          }
        
/* mul x 0 */
x *= 0;
/* add x z */
x += z;
/* mod x 26 */
x %= 26;
/* div z 26 */

          {
            const temp = z / 26;
            z = temp >= 0 ? Math.floor(temp) : Math.ceil(temp);
          }
        
/* add x -11 */
x += -11;
/* eql x w */
x = x === w ? 1 : 0;
/* eql x 0 */
x = x === 0 ? 1 : 0;
/* mul y 0 */
y *= 0;
/* add y 25 */
y += 25;
/* mul y x */
y *= x;
/* add y 1 */
y += 1;
/* mul z y */
z *= y;
/* mul y 0 */
y *= 0;
/* add y w */
y += w;
/* add y 1 */
y += 1;
/* mul y x */
y *= x;
/* add z y */
z += y;
return { w, x, y, z};
}
Done in 0.71s.
